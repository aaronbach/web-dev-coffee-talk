import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'live-coding-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
